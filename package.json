{
  "name": "obsidian-exam-plugin",
  "version": "1.0.0",
  "description": "Render exam questions as beautiful interactive cards with XML format support",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit && node esbuild.config.mjs production",
    "release": "npm run build && mkdir -p release && cp main.js manifest.json release/ && cd release && zip -r ../obsidian-exam-plugin.zip . && cd .. && rm -rf release"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "exam",
    "questions"
  ],
  "author": "zengjing",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@typescript-eslint/eslint-plugin": "5.2.0",
    "@typescript-eslint/parser": "5.2.0",
    "builtin-modules": "3.2.0",
    "esbuild": "0.13.12",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "4.7.4"
  }
}
